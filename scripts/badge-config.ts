export const BADGE_CONFIG = {
  // Semantic version for the workspace
  version: '0.1.0',

  // Percentage of propositions with FOL v1 encodings
  // (global coverage across all parts, not per-part)
  propsCoverage: 87,
};
